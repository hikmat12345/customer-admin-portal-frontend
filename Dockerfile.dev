FROM node:22-alpine
RUN apk update && apk add git openssh
WORKDIR /app
COPY package*.json ./
RUN --mount=type=ssh \
    --mount=type=bind,target=~/.ssh/known_hosts,source=known_hosts \
    npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]